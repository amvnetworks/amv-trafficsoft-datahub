description = 'A module containing spring-boot autoconfiguration classes for handling xfcd data deliveries'

dependencies {
    implementation project(':xfcd')
    implementation "io.vertx:vertx-core:${vertxVersion}"
    implementation "org.springframework.boot:spring-boot-autoconfigure:${springBootVersion}"
    // Starter provides auto-config and properties
    implementation "org.amv.trafficsoft:amv-trafficsoft-rest-client-spring-boot-starter:${trafficsoftRestVersion}"
    implementation "org.amv.trafficsoft:amv-trafficsoft-rest-client-spring-boot-autoconfigure:${trafficsoftRestVersion}"

    // Ensure client API (e.g., XfcdClient) is available on compile classpath
    implementation "org.amv.trafficsoft:amv-trafficsoft-rest-client:${trafficsoftRestVersion}"

    annotationProcessor "org.springframework.boot:spring-boot-configuration-processor:${springBootVersion}"

    testImplementation "io.vertx:vertx-unit:${vertxVersion}"
    testImplementation "org.amv.vertx:amv-vertx-spring-boot-starter:${amvVertxVersion}"
    testImplementation "io.vertx:vertx-rx-java3:${vertxVersion}"
    // Needed by tests for CollectorRegistry (align with main module version to avoid _total suffix differences)
    testImplementation 'io.prometheus:simpleclient:0.7.0'
    // Include model classes and mothers used in tests
    testImplementation "org.amv.trafficsoft:amv-trafficsoft-rest-model:${trafficsoftRestVersion}"
    testImplementation "org.amv.trafficsoft:amv-trafficsoft-rest-model-test:${trafficsoftRestVersion}"
}
